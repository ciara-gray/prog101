# Fill in the blanks so the answers are all 2. Use help for unfamiliar functions
# if necessary.
log10(100)
sqrt(4)
ceiling(1.1)
?ceiling

# Find the functions for cosine and sine. What are the cosine and sine of 0?
sin(0)
#=0
cos(0)
#=1

# What do you think the following functions do? Just go off the name, don't look
# them up.
read.csv()
#read a csv file, maybe separate columns or provide the data in those columns
boxplot()
#generate a box plot
startsWith()
#returns T/F about if something starts with something. ("", "")
